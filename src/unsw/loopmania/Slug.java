package unsw.loopmania;

/**
 * represents a slug
 */
public class Slug extends BasicEnemy {
    public Slug(PathPosition position, String name, int health, int damage) {
        super(position, name, 50, 2);
    }
}
